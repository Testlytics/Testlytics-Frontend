import React from "react";
import TableColour from "./TableColour";

export default {
  title: "Components/TableColour",
  component: TableColour,
};

const Template = (args) => <TableColour {...args} />;

export const Default = Template.bind({});
Default.args = {};
